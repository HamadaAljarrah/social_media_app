import { Register } from "../types/user"
import { SERVER_URL } from "../types/variables"

export const useSingUp = (data: Register) => {
    const url = SERVER_URL + 'auth/login'
    const response = 1;
}